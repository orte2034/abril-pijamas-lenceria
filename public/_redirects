# Netlify Redirects for SPA-style routing
# Colocar en public/_redirects

# Catálogo público
/catalogo /catalogo.html 200

# Producto detalle - captura slug y sirve producto.html
/producto/* /producto.html 200

# Admin panel
/admin /admin.html 200
/admin.html /admin.html 200

# Assets estáticos (ya servidos por defecto)
# /js/* /js/:splat 200
# /styles/* /styles/:splat 200

# SPA fallback para rutas no encontradas (opcional)
# /* /index.html 200